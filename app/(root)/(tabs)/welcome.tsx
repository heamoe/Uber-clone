import { SafeAreaView, Text } from "react-native";

const onborading = () => {
  return (
    <SafeAreaView>
      <Text>welcome to uber clone</Text>
    </SafeAreaView>
  );
};

export default onborading;
